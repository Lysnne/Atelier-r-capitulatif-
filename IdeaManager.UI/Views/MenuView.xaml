<UserControl x:Class="IdeaManager.UI.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IdeaManager.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal" Margin="10" Background="#FFB082BF">
        <Button Content="Liste des idées" 
                Command="{Binding NaviguerversListIdeesCommand}"
                Margin="0,0,10,0"
                Padding="10,5"
                Background="#FF22114F"
                Foreground="White"/>
        <Button Content="Nouvelle idée" 
                Command="{Binding NaviguerversFormIdeesCommand}"
                Padding="10,5"
                Background="#FF22114F"
                Foreground="White"/>
    </StackPanel>
</UserControl> 